/// <reference types="react" />
interface IProps {
    options: string[];
    index: number;
    disabled?: boolean;
}
export declare const ValidationType: ({ options, index, disabled }: IProps) => JSX.Element;
export {};
