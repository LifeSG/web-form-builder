import { Alert } from "@lifesg/react-design-system/alert";
import styled from "styled-components";

export const EditorAlert = styled(Alert)``;

export const Wrapper = styled.div`
    ${EditorAlert} {
        margin-bottom: 1rem;
    }
    margin-bottom: 8rem;
`;
