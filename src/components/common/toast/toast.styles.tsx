import { Color, Text, Toast } from "@lifesg/react-design-system";
import styled from "styled-components";

export const FunctionText = styled(Text.Body)`
    color: ${Color.Accent.Light[2]};
    font-size: 1rem;
`;
