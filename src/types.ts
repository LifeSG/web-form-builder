export enum EFormBuilderMode {
    ADD_FIELD = "add-field",
    EDIT_FIELD = "edit-field",
    EDIT_PAGE = "edit-page",
}

export enum EFormBuilderView {
    EDIT_FORM = "edit-form",
    SCHEMA_PREVIEW = "schema-preview",
    LISTING_PREVIEW = "listing-preview",
}
